"on":
  schedule:
    - cron: '0 22 * * *'   # 22:00 UTC = midnight Israel time (IDT)
  workflow_dispatch:

name: Fetch Jobs (Comeet + Greenhouse)

jobs:
  fetch-jobs:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests beautifulsoup4

      - name: Fetch all jobs
        run: python fetch_jobs.py

      - name: Commit CSVs to repo
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add comeet_jobs_*.csv greenhouse_jobs_*.csv lever_jobs_*.csv ashby_jobs_*.csv workable_jobs_*.csv mitam_jobs_*.csv weizmann_jobs_*.csv huji_jobs_*.csv history.csv
          git diff --staged --quiet || git commit -m "chore: jobs update $(date -u +%Y-%m-%d)"
          git push
